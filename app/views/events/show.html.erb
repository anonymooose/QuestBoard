<div class="page-wrapper event-show-wpr">
  <div class="container">
    <div class="row is-flex page-card">

      <div class="col-xs-12 event-show-heading">
        <div class="main-heading-group">
          <h2>Quest Details</h2>
          <div class="quest-buttons pull-right">
<!--             <% if current_user.host == @event.host %>
              <%= link_to '<i class="fa fa-pencil-square-o"></i>'.html_safe, edit_event_path %>
              <%= link_to '<i class="fa fa-times"></i>'.html_safe, event_path(@event), method: :delete %>
            <% end %> -->
          </div>
        </div>
      </div>

      <div class="col-sm-6 quest-show-info">
        <%= render 'eventdetails' %>
      </div>

      <div class="col-sm-6 quest-show-party">
        <%= render 'partydetails' %>
      </div>

    </div>
  </div>
</div>


